# Agent Instructions

Read `PROJECT_CONTEXT.md` before substantial work. Follow `process.md` for
non-trivial changes.

Before closeout, run:

```bash
scripts/verify quick
```

Use `smoke` when checking the basic artifact workflow and `full` for broader
confidence.
