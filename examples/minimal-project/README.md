# Minimal Project Example

This example shows the smallest useful application of the AI Development
Playbook.

It is intentionally language-neutral. Verification uses shell checks against
text files rather than Python, TypeScript, or another implementation stack.

Run:

```bash
scripts/verify quick
```
